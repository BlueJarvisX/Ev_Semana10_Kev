<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- REMOVIMOS package del manifest -->

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- PERMISO PERSONALIZADO PARA SEGURIDAD -->
    <permission
        android:name="com.example.gpsgooglemapskev.PERMISSION_PRIVATE"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GpsGoogleMapsKev"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:ignore="HardcodedDebugMode,GoogleAppIndexingWarning">

        <!-- Actividad principal/menu -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Actividad GoogleMaps -->
        <activity
            android:name=".MapsActivity"
            android:exported="false" />
        <!-- Actividad Hilos -->
        <activity
            android:name=".HilosSensoresActivity"
            android:exported="false" />

        <!-- ✅ API KEY FUNCIONAL - MANTENEMOS LA QUE SIRVE -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCiSyECVSyexJq5xQDj8vnKncFVXQAb4vE" />
    </application>
</manifest>